const anotherNumber = 1;
const validate = anotherNumber ?? 5; //with ?? with ask if anotherNumber is null, if is null we set the value 5.
console.log(validate)

const aText = 'darvin';
const anotherValidate = aText ?? "Emil";

console.log(anotherValidate)
const user = {
    thedeltadr:{
        country: 'DOM'
    },
    adriana:{
        country: 'VEN'
    }
}

// console.log(user.thedeltadr);

// using ? front the element we say like "if this exists access to this, if doens't exist return undefined"
console.log(user?.darvin?.country)